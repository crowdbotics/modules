{
  "title": "App Menu",
  "description": "Module that shows available routes in a menu with links",
  "root": "/modules/app-menu",
  "schema": {
    "title": {
      "type": "string",
      "minLength": 3,
      "maxLength": 12,
      "pattern": "^[A-Z]"
    },
    "copy": {
      "type": "string",
      "minLength": 4,
      "pattern": "^[A-Z]"
    }
  }
}
